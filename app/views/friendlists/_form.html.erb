<%= form_with(model: friendlist, local: true) do |form| %>
  <% if friendlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friendlist.errors.count, "error") %> prohibited this friendlist from being saved:</h2>

      <ul>
        <% friendlist.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
